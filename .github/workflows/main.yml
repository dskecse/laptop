name: build

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - run: brew update
      - run: brew install shellcheck
      - run: shellcheck mac
      - run: sh mac
