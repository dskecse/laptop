name: build

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - run: brew update
      - run: brew install shellcheck
      - run: shellcheck laptop.local
      - run: sh laptop.local
