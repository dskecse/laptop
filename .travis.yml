os: osx
osx_image: xcode12.2
addons:
  homebrew:
    packages:
    - shellcheck
    update: true

language: sh

script:
  - shellcheck laptop.local
  - sh laptop.local

notifications:
  email: false

branches:
  only:
    - master
